@model ElibForumMVC.Models.ApplicationUser.ProfileList

<div class="container body-content">
    <div class="row sectionHeader">
        <div class="sectionHeading">Корисници</div>
    </div>
    <div class="row" id="forumIndexContent">
        <table class="table table-hover" id="forumIndexTable">
            <thead>
                <tr>
                    <th>
                        Профилна Слика
                    </th>
                    <th>
                        Корисничко Име
                    </th>
                    <th>
                        Член од
                    </th>
                    <th>
                        E-Пошта
                    </th>
                    <th>
                        Кориснички Рејтинг
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var profile in Model.Profiles)
                {
                    <tr class="userRow">
                        <td>
                            <div class="profile-img" id="userProfileImage" style="background-image: url(@profile.ProfileImageUrl); background-size:100%"></div>
                        </td>
                        <td>
                            <a asp-controller="Profile" asp-action="Detail" , asp-route-id="@profile.UserId">
                                @profile.UserName
                            </a>
                        </td>
                        <td>
                            @profile.MemberSince
                        </td>
                        <td>
                            @profile.Email
                        </td>
                        <td>
                        
                        </td>
                        <td>
                            @profile.UserRating
                        </td>
                    </tr>
                }

            </tbody>
        </table>
    </div>
</div>