
@using ElibForumMVC.Data.Models
@using Microsoft.AspNetCore.Identity
@model ElibForumMVC.Models.ApplicationUser.ProfileModel

@inject UserManager<ApplicationUser> UserManager

@{
    
    //var user = UserManager.FindByNameAsync(User.Identity.Name);
    //var isAdmin = user.Result.IsAdmin;
}

<div class="container body-content">
    <div class="row sectionHeader">
        <div class="sectionHeading noBorder">
           Кориснички Профил
        </div>
        @*@if (User.IsInRole("Admin"))
        {
            <form method="post" asp-controller="Profile" asp-action="Deactivate" asp-route-id="@Model.UserId">
                <div>
                    <button class="btn btn-danger" active="@Model.IsActive">Deactivate User</button>
                </div>
            </form>
        }*@
    </div>
    @if (User.Identity.IsAuthenticated)
    {
        <div class="row userProfile">
            <div class="col-md-4">
                <div id="userProfileImage" style="background-image: url(@Model.ProfileImageUrl); background-size:100%;"></div>
                @if (User.Identity.Name == Model.UserName)
                {
                    <form method="post" enctype="multipart/form-data" asp-controller="Profile" asp-action="UploadProfileImage">
                        <div id="upload">
                            <label class="btn btn-default btn-file">
                                Отвори <input type="file" name="file" style="display: none;">
                            </label>
                        </div>
                        <button type="submit" id="btn-profile" class="btn btn-default">Испрати</button>
                    </form>
                }
            </div>
            <div class="col-md-8">
                <span id="userName">@Model.UserName</span>
                @if (@Model.IsAdmin)
                {
                    <span class="isAdmin">Админ</span>
                }
                <span id="userRating">Моментален Рејтинг: @Model.UserRating</span>
                <span id="userEmailLabel">Емаил: @Model.Email</span>
                <span id="userCreatedLabel">Член од: @Model.MemberSince</span>
                <span id="userActiveLabel">
                    Status:
                    <span>Активен</span>
                    @*@if (!@Model.IsActive)
                    {
                        <span class="inactiveUser">Inactive</span>
                    }
                    else
                    {
                        <span>Active</span>
                    }*@
                </span>
            </div>
        </div>
    }
</div>